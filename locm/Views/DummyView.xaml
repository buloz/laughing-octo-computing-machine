<UserControl x:Class="locm.Views.DummyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:locm.Views"
             mc:Ignorable="d">
    <Border Background="Transparent" BorderBrush="LightSkyBlue"  BorderThickness="1" CornerRadius="10" Padding="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox Name="TextField" 
                     Grid.Row="0" MinWidth="200" Margin="5"/>
            <Button Name="SuperButton" 
                    Grid.Row="1" HorizontalAlignment="Center" Padding="3"
                    Content="Clique moi" />
        </Grid>
    </Border>
</UserControl>
